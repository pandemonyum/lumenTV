# Copiare in .env.development. Letto da Vite solo per "npm run dev" (vite senza --mode), con
# priorita piu alta di .env: azzera VITE_API_BASE_URL cosi il test locale nel browser punta
# sempre a localhost:8787 invece di ereditare l'IP LAN pensato per LG/iOS/Android in .env.
VITE_API_BASE_URL=
